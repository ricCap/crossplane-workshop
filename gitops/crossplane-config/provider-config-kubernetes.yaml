# In-cluster identity for provider-kubernetes — creates Ingress and
# ResourceQuota objects on the management cluster.
apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: InjectedIdentity
