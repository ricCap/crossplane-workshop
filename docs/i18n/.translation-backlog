# Translation backlog — English paths (relative to docs/docs/) that don't
# yet have an Italian translation. The CI sync check skips these.
#
# When you add a translation under
#     docs/i18n/it/docusaurus-plugin-content-docs/current/<path>
# remove the matching line from this file in the same commit.
#
# When you create a new English page, either translate it in the same PR
# or add the path here (a deliberate decision, visible in code review).
#
# The backlog is currently empty — every English doc has an Italian
# translation. See AGENTS.md §Localization.
